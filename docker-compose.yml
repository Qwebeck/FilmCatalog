version: "3"
services:
    web:
        build: .
        ports:
            - "5001:5001"
        depends_on:
            - db
    db:
        image: "mcr.microsoft.com/mssql/server"
        environment:
            SA_PASSWORD: "Strong_Password123456"
            ACCEPT_EULA: "Y"

